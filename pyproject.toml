[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "jarvis-assistant"
version = "0.1.0"
description = "Personal voice-controlled desktop assistant for Windows"
requires-python = ">=3.13"

[tool.setuptools.packages.find]
where = ["src"]